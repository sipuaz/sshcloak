{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"sshcloak","text":"<p>sshcloak is a transparent SSH credential manager for Linux and macOS. It keeps your <code>~/.ssh/config</code> exactly as you wrote it while silently injecting passwords and managing host entries through a dedicated include file backed by an age-encrypted vault.</p> <p><code>ssh myserver</code></p> <p>That's it. No password prompt. No proprietary config. No subscription.</p>"},{"location":"#overview","title":"Overview","text":"<pre><code>flowchart LR\n    subgraph User\n        A([ssh myserver])\n    end\n    subgraph sshcloak\n        B[config manager\\n~/.ssh/sshcloak/config]\n        C[vault\\n~/.ssh/sshcloak/vault.age]\n    end\n    subgraph OpenSSH\n        D([/usr/bin/ssh])\n    end\n\n    A --&gt;|host lookup| B\n    A --&gt;|credential lookup| C\n    C --&gt;|password| D\n    B --&gt;|HostName / User / Port| D</code></pre>"},{"location":"#key-properties","title":"Key properties","text":"Property Detail No root required Everything lives under <code>~/.ssh/sshcloak/</code> Non-destructive Only one <code>Include</code> line is ever appended to <code>~/.ssh/config</code> Encrypted at rest Vault uses age with scrypt passphrase derivation Zero dependencies at runtime Single statically-linked binary Standard SSH config Host entries are valid <code>ssh_config(5)</code> \u2014 readable by any tool"},{"location":"#requirements","title":"Requirements","text":""},{"location":"#runtime","title":"Runtime","text":"Requirement Version Linux or macOS \u2014 OpenSSH client (<code>ssh</code>) \u2265 7.3 (for <code>Include</code> directive support)"},{"location":"#build","title":"Build","text":"Requirement Version Go \u2265 1.22"},{"location":"#optional-for-docs","title":"Optional (for docs)","text":"Requirement Version Python \u2265 3.9 MkDocs + Material see <code>docs/requirements.txt</code>"},{"location":"#project-layout","title":"Project layout","text":"<p><code>sshcloak/ \u251c\u2500\u2500 cmd/ \u2502   \u251c\u2500\u2500 main.go                     # entry point \u2502   \u2514\u2500\u2500 internal/cli/ \u2502       \u251c\u2500\u2500 root.go                 # root command, flag wiring \u2502       \u251c\u2500\u2500 host/                   # sshcloak host * \u2502       \u251c\u2500\u2500 vault/                  # sshcloak vault * \u2502       \u2514\u2500\u2500 password/               # sshcloak password * \u251c\u2500\u2500 internal/ \u2502   \u251c\u2500\u2500 config/                     # SSH config parser + CRUD manager \u2502   \u2514\u2500\u2500 keyring/                    # age-encrypted vault \u251c\u2500\u2500 docs/                           # this documentation \u251c\u2500\u2500 Makefile \u2514\u2500\u2500 mkdocs.yml</code></p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#from-source-recommended","title":"From source (recommended)","text":"<p>To install <code>sshcloak</code> as a global command for your user account without sudo, use the user-local target:</p> <p><code>bash git clone https://github.com/sipuaz/sshcloak.git cd sshcloak make install-user export PATH=\"$HOME/.local/bin:$PATH\"</code></p> <p>For a system-wide install:</p> <p><code>bash git clone https://github.com/sipuaz/sshcloak.git cd sshcloak make install          # builds and copies to /usr/local/bin/sshcloak</code></p> <p><code>make install</code> requires write permission to <code>/usr/local/bin</code>. Use <code>sudo make install</code> if you do not own that directory.</p>"},{"location":"getting-started/#with-go-install","title":"With <code>go install</code>","text":"<p><code>bash go install github.com/sipuaz/sshcloak/cmd@latest</code></p> <p>The binary is placed in <code>$GOPATH/bin</code> (default <code>~/go/bin</code>). Ensure that directory is on your <code>$PATH</code>:</p> <p><code>bash export PATH=\"$HOME/go/bin:$PATH\"   # add to ~/.zshrc or ~/.bashrc</code></p> <p>You can also check the installed binary with:</p> <p><code>bash sshcloak version sshcloak --help</code></p>"},{"location":"getting-started/#first-run","title":"First run","text":""},{"location":"getting-started/#1-bootstrap-sshcloak","title":"1 \u2014 Bootstrap sshcloak","text":"<p>The bootstrap command creates the <code>Include ~/.ssh/sshcloak/config</code> line in <code>~/.ssh/config</code> and initialises the encrypted vault.  Create it with a passphrase you will remember:</p> <p><code>$ sshcloak init Enter sshcloak vault passphrase: Confirm passphrase: sshcloak initialised</code></p> <p>The vault is written to <code>~/.ssh/sshcloak/vault.age</code>.</p> <p>By default <code>sshcloak init</code> places the <code>Include</code> directive at the top of <code>~/.ssh/config</code> so that sshcloak host entries take precedence over any existing entries (SSH uses first-match-wins semantics).  If the directive was previously placed at the bottom (for example by an earlier <code>sshcloak host add</code>), it is automatically relocated to the top.</p> <p>If you want the Include directive appended instead, use <code>--append</code>:</p> <p><code>bash sshcloak init --append</code></p> <p>If you prefer a local shortcut during development, <code>make init</code> runs the same bootstrap flow after building the binary.</p>"},{"location":"getting-started/#2-add-a-host","title":"2 \u2014 Add a host","text":"<p><code>$ sshcloak host add myserver \\     --hostname 203.0.113.10 \\     --user alice \\     --port 2222 host \"myserver\" added</code></p> <p>sshcloak adds one <code>Include ~/.ssh/sshcloak/config</code> line at the top of <code>~/.ssh/config</code> if the bootstrap command has not already done it, ensuring sshcloak hosts are resolved with the highest priority.</p>"},{"location":"getting-started/#3-store-the-password","title":"3 \u2014 Store the password","text":"<p><code>$ sshcloak password set myserver Vault passphrase: SSH password for \"myserver\": password stored for \"myserver\"</code></p>"},{"location":"getting-started/#4-verify","title":"4 \u2014 Verify","text":"<p><code>$ sshcloak host get myserver Label:    myserver HostName: 203.0.113.10 User:     alice Port:     2222</code></p> <p><code>$ ssh myserver     # OpenSSH resolves the host via the include file</code></p>"},{"location":"getting-started/#file-locations","title":"File locations","text":"File Default path Purpose Root SSH config <code>~/.ssh/config</code> Untouched except for one <code>Include</code> line Managed config <code>~/.ssh/sshcloak/config</code> All sshcloak host entries Metadata sidecar <code>~/.ssh/sshcloak/meta.yaml</code> Host tags and future sshcloak-owned metadata Vault <code>~/.ssh/sshcloak/vault.age</code> age-encrypted password store <p>All paths can be overridden with global flags \u2014 see <code>sshcloak --help</code>.</p>"},{"location":"getting-started/#uninstalling","title":"Uninstalling","text":"<p><code>bash sudo make uninstall          # removes /usr/local/bin/sshcloak rm -rf ~/.ssh/sshcloak       # removes managed config + vault</code></p> <p>Then manually remove the <code>Include ~/.ssh/sshcloak/config</code> line from <code>~/.ssh/config</code>.</p>"},{"location":"internals/","title":"Internals","text":"<p>-.-# Internals</p> <p>This page describes sshcloak's internal architecture for contributors and advanced users.</p>"},{"location":"internals/#component-overview","title":"Component overview","text":"<pre><code>graph TD\n    subgraph cmd\n        ROOT[root.go flags + wiring]\n        HOST[host/* add list get edit remove]\n        VAULT[vault/* init rotate-passphrase]\n        PASS[password/* set get delete list]\n    end\n\n    subgraph internal/config\n        MGR[Manager CRUD + include bootstrap]\n        PARSER[Parser tokenizer \u2192 AST]\n        EVAL[Evaluator first-match-wins resolve]\n        FILE[FileHandler atomic read/write]\n    end\n\n    subgraph internal/keyring\n        STORE[FileVaultStore lock / unlock / CRUD]\n        CRYPTO[crypto.go age encrypt / decrypt]\n        TYPES[types.go SecretRecord VaultDocument]\n    end\n\n    ROOT --&gt; HOST\n    ROOT --&gt; VAULT\n    ROOT --&gt; PASS\n\n    HOST --&gt; MGR\n    VAULT --&gt; STORE\n    PASS --&gt; STORE\n\n    MGR --&gt; PARSER\n    MGR --&gt; EVAL\n    MGR --&gt; FILE\n\n    STORE --&gt; CRYPTO\n    STORE --&gt; TYPES\n    CRYPTO --&gt;|filippo.io/age| EXT([age library])</code></pre>"},{"location":"internals/#ssh-config-manager-internalconfig","title":"SSH config manager (<code>internal/config</code>)","text":"<p>sshcloak parses <code>~/.ssh/config</code> with a hand-written tokenizer and builds an AST of <code>Host</code> and <code>Match</code> blocks.  It never uses an external SSH config library so the output is always canonical and round-trips losslessly.</p>"},{"location":"internals/#parse-pipeline","title":"Parse pipeline","text":"<pre><code>flowchart LR\n    A[raw text] --&gt; B[Lexer token stream]\n    B --&gt; C[Parser AST: Config / Block / Directive]\n    C --&gt; D[Evaluator Resolve hostname \u2192 directives]\n    C --&gt; E[Renderer AST \u2192 canonical text]\n    E --&gt; F[FileHandler atomic write]</code></pre>"},{"location":"internals/#isolation-model","title":"Isolation model","text":"<p>sshcloak appends exactly one line to <code>~/.ssh/config</code>:</p> <p><code>Include ~/.ssh/sshcloak/config</code></p> <p>All host CRUD operates exclusively on <code>~/.ssh/sshcloak/config</code>.  The root file is never rewritten, only appended to (once).</p>"},{"location":"internals/#atomic-writes","title":"Atomic writes","text":"<p>All config and vault writes use the same pattern:</p> <ol> <li>Write to a temp file in the same directory as the target.</li> <li><code>chmod</code> the temp file to the correct permissions.</li> <li><code>os.Rename</code> \u2014 atomic on POSIX systems.</li> </ol> <p>This guarantees that a crash or power loss never produces a truncated or partially-written file.</p>"},{"location":"internals/#encrypted-vault-internalkeyring","title":"Encrypted vault (<code>internal/keyring</code>)","text":""},{"location":"internals/#vault-format","title":"Vault format","text":"<p>The vault is a single YAML document encrypted with age and ASCII-armored (PEM-style).  The plaintext structure:</p> <p><code>yaml version: 1 secrets:   \"myserver\\x00password\":     label: myserver     kind: password     value: s3cr3t!     created_at: 2026-05-10T12:00:00Z     updated_at: 2026-05-10T12:00:00Z</code></p> <p>The map key is <code>label + \"\\x00\" + kind</code>, which makes label+kind pairs unique without nesting.</p>"},{"location":"internals/#encryption","title":"Encryption","text":"<pre><code>sequenceDiagram\n    participant CLI\n    participant FileVaultStore\n    participant age\n\n    CLI-&gt;&gt;FileVaultStore: Unlock(passphrase)\n    FileVaultStore-&gt;&gt;age: NewScryptIdentity(passphrase)\n    age--&gt;&gt;FileVaultStore: decrypt vault.age \u2192 VaultDocument\n    FileVaultStore--&gt;&gt;CLI: ok\n\n    CLI-&gt;&gt;FileVaultStore: Put(record)\n    FileVaultStore-&gt;&gt;age: NewScryptRecipient(passphrase)\n    age--&gt;&gt;FileVaultStore: encrypt VaultDocument \u2192 vault.age\n    FileVaultStore--&gt;&gt;CLI: ok\n\n    CLI-&gt;&gt;FileVaultStore: Lock()\n    FileVaultStore-&gt;&gt;FileVaultStore: zero passphrase bytes</code></pre> <p>age uses scrypt with default work factors, which makes brute-force attacks against a stolen vault file computationally expensive.</p>"},{"location":"internals/#data-flow-password-set","title":"Data flow: <code>password set</code>","text":"<pre><code>sequenceDiagram\n    actor User\n    participant CLI as sshcloak password set\n    participant Vault as FileVaultStore\n    participant Disk\n\n    User-&gt;&gt;CLI: sshcloak password set myserver\n    CLI-&gt;&gt;User: Vault passphrase: [no echo]\n    User-&gt;&gt;CLI: \u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\n    CLI-&gt;&gt;Vault: Unlock(passphrase)\n    Vault-&gt;&gt;Disk: read vault.age\n    Disk--&gt;&gt;Vault: ciphertext\n    Vault-&gt;&gt;Vault: age decrypt \u2192 VaultDocument\n    Vault--&gt;&gt;CLI: ok\n\n    CLI-&gt;&gt;User: SSH password for \"myserver\": [no echo]\n    User-&gt;&gt;CLI: \u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\n    CLI-&gt;&gt;Vault: Put(SecretRecord{label, password, value})\n    Vault-&gt;&gt;Vault: age encrypt \u2192 ciphertext\n    Vault-&gt;&gt;Disk: atomic write vault.age\n    Vault--&gt;&gt;CLI: ok\n\n    CLI-&gt;&gt;Vault: Lock()\n    Vault-&gt;&gt;Vault: zero passphrase bytes in memory\n    CLI--&gt;&gt;User: password stored for \"myserver\"</code></pre>"},{"location":"internals/#dependency-graph","title":"Dependency graph","text":"Package External dependency Purpose <code>internal/keyring</code> <code>filippo.io/age</code> Passphrase-based encryption <code>internal/keyring</code> <code>go.yaml.in/yaml/v3</code> Vault serialization <code>cmd/...</code> <code>github.com/spf13/cobra</code> CLI framework <code>cmd/...</code> <code>golang.org/x/term</code> No-echo passphrase prompting <code>internal/config</code> none Pure Go SSH config parser"},{"location":"commands/connect/","title":"connect","text":"<p>Open an SSH session with automatic password injection.</p>"},{"location":"commands/connect/#synopsis","title":"Synopsis","text":"<p><code>sshcloak connect [&lt;label&gt;] [-- &lt;ssh-args&gt;...]</code></p>"},{"location":"commands/connect/#description","title":"Description","text":"<p><code>sshcloak connect</code> retrieves the stored password for <code>&lt;label&gt;</code> from the encrypted vault and opens an SSH session to that host without requiring you to type the password manually. If no label is provided, sshcloak shows an interactive host picker; <code>--tag</code> narrows that picker to hosts carrying one metadata tag.</p> <p>Internally the command executes:</p> <p><code>sshpass -e ssh &lt;label&gt; [ssh-args...]</code></p> <p>The password is passed to <code>sshpass</code> via the <code>SSHPASS</code> environment variable so it does not appear in the process list (unlike <code>sshpass -p</code>).</p> <p>Because <code>sshcloak connect</code> replaces the current process with <code>sshpass</code> + <code>ssh</code> (via <code>execve</code>), the SSH session inherits the terminal directly \u2014 PTY allocation, signal forwarding, and terminal resize all work as expected.</p>"},{"location":"commands/connect/#prerequisites","title":"Prerequisites","text":"<p><code>sshpass</code> must be installed and on your <code>$PATH</code>:</p> Distribution Command Debian / Ubuntu <code>apt install sshpass</code> Fedora / RHEL <code>dnf install sshpass</code> macOS (Homebrew) <code>brew install hudochenkov/sshpass/sshpass</code>"},{"location":"commands/connect/#arguments","title":"Arguments","text":"Argument Description <code>&lt;label&gt;</code> Optional sshcloak host label whose password should be injected <code>-- &lt;ssh-args&gt;</code> Optional extra arguments forwarded verbatim to <code>ssh</code>"},{"location":"commands/connect/#flags","title":"Flags","text":"Flag Description <code>--tag &lt;name&gt;</code> Filter the interactive picker by one tag when no label is provided <code>--debug &lt;0-3&gt;</code> Add <code>-v</code>, <code>-vv</code>, or <code>-vvv</code> to the underlying <code>ssh</code> invocation"},{"location":"commands/connect/#examples","title":"Examples","text":"<p>Basic connection:</p> <p><code>bash sshcloak connect prod</code></p> <p>Interactive selection filtered by tag:</p> <p><code>bash sshcloak connect --tag stable</code></p> <p>With extra ssh flags (port forwarding, X11 forwarding):</p> <p><code>bash sshcloak connect prod -- -X -L 8080:localhost:80</code></p>"},{"location":"commands/connect/#workflow","title":"Workflow","text":"<p><code>sshcloak connect prod</code></p> <ol> <li>Resolves the host label directly, or via the interactive picker when no label was given.</li> <li>If <code>--tag</code> was provided, filters the picker to hosts carrying that tag.</li> <li>Prompts for the vault passphrase (no echo).</li> <li>Unlocks the encrypted vault.</li> <li>Retrieves the password stored under the selected label.</li> <li>Execs: <code>sshpass -e ssh &lt;label&gt;</code> with <code>SSHPASS=&lt;password&gt;</code> in the environment.</li> </ol>"},{"location":"commands/connect/#notes","title":"Notes","text":"<ul> <li>A password must already be stored with <code>sshcloak password set &lt;label&gt;</code> before   <code>connect</code> can inject it.</li> <li>The host entry must be resolvable by SSH \u2014 either via the sshcloak-managed   include file or your existing <code>~/.ssh/config</code>.</li> <li><code>connect</code> does not fall back to interactive password entry if no vault record   exists; it exits with an error so that <code>sshpass</code> is never invoked without a   known password.</li> <li>To use key-based authentication without password injection, connect with   plain <code>ssh &lt;label&gt;</code> as usual.</li> </ul>"},{"location":"commands/host/","title":"sshcloak host","text":"<p>Manage SSH host entries in the sshcloak-managed include file.</p>"},{"location":"commands/host/#synopsis","title":"Synopsis","text":"<p><code>sshcloak host &lt;subcommand&gt; [options]</code></p>"},{"location":"commands/host/#subcommands","title":"Subcommands","text":""},{"location":"commands/host/#host-add","title":"host add","text":"<p><code>sshcloak host add &lt;label&gt; [flags]</code></p> <p>Description</p> <p>Add a new <code>Host</code> block to <code>~/.ssh/sshcloak/config</code>.  The <code>label</code> becomes the SSH alias (the value after <code>Host</code> in the config file).  Wildcards are not permitted as labels.</p> <p>If <code>~/.ssh/config</code> does not yet contain an <code>Include</code> directive for the managed file it is added automatically.</p> <p>Options</p> Flag Type Description <code>--hostname</code> string Remote hostname or IP address (<code>HostName</code>) <code>--user</code> string Remote login username (<code>User</code>) <code>--port</code> string Remote port (<code>Port</code>) <code>--identity-file</code> string (repeatable) Path to a private key (<code>IdentityFile</code>) <code>--extra KEY=VALUE</code> string (repeatable) Any additional SSH directive <p>Examples</p> <p>```bash</p>"},{"location":"commands/host/#minimal-alias-only","title":"Minimal \u2014 alias only","text":"<p>sshcloak host add bastion</p>"},{"location":"commands/host/#full-example","title":"Full example","text":"<p>sshcloak host add prod \\     --hostname 203.0.113.5 \\     --user deploy \\     --port 22 \\     --identity-file ~/.ssh/id_ed25519 \\     --extra ProxyJump=bastion \\     --extra ServerAliveInterval=30 ```</p>"},{"location":"commands/host/#host-list","title":"host list","text":"<p><code>sshcloak host list sshcloak host list --tag production</code></p> <p>Description</p> <p>Print a table of all managed host entries.  Wildcard blocks (e.g. <code>Host *</code>) are excluded from the output.  When <code>--tag</code> is provided, only hosts whose metadata sidecar entry includes that tag are shown.</p> <p>Output columns</p> Column Description <code>LABEL</code> SSH alias <code>HOSTNAME</code> <code>HostName</code> value <code>USER</code> <code>User</code> value <code>PORT</code> <code>Port</code> value <code>TAGS</code> Comma-separated tags from <code>~/.ssh/sshcloak/meta.yaml</code> <p>Example</p> <p><code>LABEL     HOSTNAME       USER    PORT  TAGS bastion   203.0.113.1    ops     22    edge,jump prod      203.0.113.5    deploy  22    production,web</code></p>"},{"location":"commands/host/#host-get","title":"host get","text":"<p><code>sshcloak host get &lt;label&gt;</code></p> <p>Description</p> <p>Print all directives for a single host entry, one per line.</p> <p>Example</p> <p><code>bash $ sshcloak host get prod Label:          prod HostName:       203.0.113.5 User:           deploy Port:           22 Tags:           production,web IdentityFile:   ~/.ssh/id_ed25519 ProxyJump:      bastion</code></p>"},{"location":"commands/host/#host-tag-add","title":"host tag add","text":"<p><code>sshcloak host tag add &lt;label&gt; &lt;tag&gt; [&lt;tag&gt;...]</code></p> <p>Description</p> <p>Add one or more arbitrary tags to a host label in the sshcloak metadata sidecar file at <code>~/.ssh/sshcloak/meta.yaml</code>.  The host itself must already exist in the SSH config tree.</p> <p>Example</p> <p><code>bash sshcloak host tag add prod production web europe</code></p>"},{"location":"commands/host/#host-tag-remove","title":"host tag remove","text":"<p><code>sshcloak host tag remove &lt;label&gt; &lt;tag&gt; [&lt;tag&gt;...]</code></p> <p>Description</p> <p>Remove one or more tags from a host label.  When the last tag is removed, the sidecar entry is deleted automatically.</p> <p>Example</p> <p><code>bash sshcloak host tag remove prod europe</code></p>"},{"location":"commands/host/#host-tag-list","title":"host tag list","text":"<p><code>sshcloak host tag list &lt;label&gt;</code></p> <p>Description</p> <p>Print the tags currently associated with one host label, one tag per line.</p> <p>Example</p> <p><code>bash sshcloak host tag list prod production web</code></p>"},{"location":"commands/host/#host-edit","title":"host edit","text":"<p><code>sshcloak host edit &lt;label&gt; [flags]</code></p> <p>Description</p> <p>Update one or more directives of an existing host entry.  Uses merge-patch semantics: only flags that are explicitly supplied overwrite the current value. Omitted flags leave the existing directive unchanged.</p> <p>To clear a single-valued field pass an empty string:</p> <p><code>bash sshcloak host edit prod --port \"\"</code></p> <p>To fully replace the <code>IdentityFile</code> list supply all desired paths via repeated <code>--identity-file</code> flags.</p> <p>Extra directives are merged: new keys overwrite existing ones, unmentioned keys are preserved.</p> <p>Options</p> <p>Same flags as <code>host add</code>.</p> <p>Example</p> <p>```bash</p>"},{"location":"commands/host/#change-only-the-port","title":"Change only the port","text":"<p>sshcloak host edit prod --port 2222</p>"},{"location":"commands/host/#add-a-proxy-jump-without-touching-other-fields","title":"Add a proxy jump without touching other fields","text":"<p>sshcloak host edit prod --extra ProxyJump=bastion ```</p>"},{"location":"commands/host/#host-remove","title":"host remove","text":"<p><code>sshcloak host remove &lt;label&gt; sshcloak host rm &lt;label&gt; sshcloak host delete &lt;label&gt;</code></p> <p>Description</p> <p>Delete the <code>Host</code> block for <code>&lt;label&gt;</code> from the managed include file. The root <code>~/.ssh/config</code> is not modified.</p> <p>Example</p> <p><code>bash sshcloak host remove prod</code></p>"},{"location":"commands/host/#global-flags","title":"Global flags","text":"<p>These flags are inherited from the root command and apply to all <code>host</code> subcommands.</p> Flag Default Description <code>--config</code> <code>~/.ssh/config</code> Path to the root SSH config file <code>--managed-config</code> <code>~/.ssh/sshcloak/config</code> Path to the sshcloak-managed include file <code>--meta</code> <code>~/.ssh/sshcloak/meta.yaml</code> Path to the sshcloak host metadata sidecar file"},{"location":"commands/host/#exit-status","title":"Exit status","text":"Code Meaning <code>0</code> Success <code>1</code> Host not found, already exists, or other error"},{"location":"commands/host/#see-also","title":"See also","text":"<ul> <li>vault \u2014 manage the encrypted vault</li> <li>password \u2014 store SSH passwords</li> </ul>"},{"location":"commands/password/","title":"sshcloak password","text":"<p>Store, retrieve, and delete SSH passwords in the encrypted vault.</p>"},{"location":"commands/password/#synopsis","title":"Synopsis","text":"<p><code>sshcloak password &lt;subcommand&gt; [label]</code></p>"},{"location":"commands/password/#description","title":"Description","text":"<p>Each password is associated with a host <code>label</code> \u2014 the same alias used in <code>sshcloak host add</code>.  Every subcommand prompts for the vault passphrase interactively, unlocks the vault, performs its operation, and locks the vault before returning.</p>"},{"location":"commands/password/#subcommands","title":"Subcommands","text":""},{"location":"commands/password/#password-set","title":"password set","text":"<p><code>sshcloak password set &lt;label&gt;</code></p> <p>Description</p> <p>Store an SSH password for <code>&lt;label&gt;</code>.  If an entry for that label already exists it is overwritten.</p> <p>Two prompts are shown in sequence (no echo on either):</p> <ol> <li>Vault passphrase \u2014 unlocks the vault</li> <li>SSH password \u2014 the value to store</li> </ol> <p>Example</p> <p><code>$ sshcloak password set prod Vault passphrase: SSH password for \"prod\": password stored for \"prod\"</code></p>"},{"location":"commands/password/#password-get","title":"password get","text":"<p><code>sshcloak password get &lt;label&gt;</code></p> <p>Description</p> <p>Print the stored password for <code>&lt;label&gt;</code> to stdout followed by a newline. The vault passphrase is prompted on stderr so the password alone can be captured in a sub-shell.</p> <p>Security</p> <p>The password is printed in plaintext.  Do not use this command in environments where stdout is logged or visible to other users.</p> <p>Example</p> <p><code>bash $ sshcloak password get prod Vault passphrase: s3cr3t!</code></p> <p>Scripting:</p> <p><code>bash PASS=$(sshcloak password get prod) sshpass -p \"$PASS\" ssh prod</code></p>"},{"location":"commands/password/#password-delete","title":"password delete","text":"<p><code>sshcloak password delete &lt;label&gt; sshcloak password rm &lt;label&gt; sshcloak password remove &lt;label&gt;</code></p> <p>Description</p> <p>Remove the stored password for <code>&lt;label&gt;</code> from the vault.  The vault is re-encrypted after deletion.</p> <p>Example</p> <p><code>$ sshcloak password delete prod Vault passphrase: password deleted for \"prod\"</code></p>"},{"location":"commands/password/#password-list","title":"password list","text":"<p><code>sshcloak password list</code></p> <p>Description</p> <p>Print the label of every entry in the vault that has kind <code>password</code>, one per line.  If no passwords are stored, prints <code>no passwords stored</code>.</p> <p>Example</p> <p><code>$ sshcloak password list Vault passphrase: bastion prod staging</code></p>"},{"location":"commands/password/#global-flags","title":"Global flags","text":"Flag Default Description <code>--vault</code> <code>~/.ssh/sshcloak/vault.age</code> Path to the encrypted vault file"},{"location":"commands/password/#exit-status","title":"Exit status","text":"Code Meaning <code>0</code> Success <code>1</code> Wrong passphrase, label not found, or I/O error"},{"location":"commands/password/#see-also","title":"See also","text":"<ul> <li>vault \u2014 initialise and rotate the vault passphrase</li> <li>host \u2014 manage SSH host entries</li> </ul>"},{"location":"commands/vault/","title":"sshcloak vault","text":"<p>Manage the age-encrypted secret vault.</p>"},{"location":"commands/vault/#synopsis","title":"Synopsis","text":"<p><code>sshcloak vault &lt;subcommand&gt;</code></p>"},{"location":"commands/vault/#description","title":"Description","text":"<p>The vault is a single file encrypted with age using scrypt passphrase derivation.  It stores all SSH passwords managed by sshcloak.  The default location is <code>~/.ssh/sshcloak/vault.age</code>.</p> <p>The vault is locked at rest.  Every command that reads or writes secrets prompts for the passphrase, unlocks the vault in memory, performs its operation, then locks the vault again before exiting.</p> <p>For a fresh installation, use <code>sshcloak init</code> to bootstrap both the SSH include file and the vault in one step.</p>"},{"location":"commands/vault/#subcommands","title":"Subcommands","text":""},{"location":"commands/vault/#vault-init","title":"vault init","text":"<p><code>sshcloak vault init</code></p> <p>Description</p> <p>Create the vault file if it does not exist, or verify an existing one by performing an unlock/lock round-trip.</p> <p>The passphrase is prompted twice for confirmation (no echo).</p> <p>Running <code>vault init</code> is optional.  The first <code>password set</code> call also creates the vault automatically, but it does not configure the SSH include line.</p> <p>Example</p> <p><code>$ sshcloak vault init Enter vault passphrase: Confirm passphrase: vault initialised</code></p>"},{"location":"commands/vault/#vault-rotate-passphrase","title":"vault rotate-passphrase","text":"<p><code>sshcloak vault rotate-passphrase</code></p> <p>Description</p> <p>Unlock the vault with the current passphrase and immediately re-encrypt it with a new one.  The vault contents are not modified \u2014 only the encryption key changes.</p> <p>Both the current and the new passphrase are read interactively.  The new passphrase is prompted twice for confirmation.</p> <p>Example</p> <p><code>$ sshcloak vault rotate-passphrase Current passphrase: New passphrase: Confirm new passphrase: vault passphrase rotated</code></p>"},{"location":"commands/vault/#global-flags","title":"Global flags","text":"Flag Default Description <code>--vault</code> <code>~/.ssh/sshcloak/vault.age</code> Path to the encrypted vault file"},{"location":"commands/vault/#exit-status","title":"Exit status","text":"Code Meaning <code>0</code> Success <code>1</code> Wrong passphrase, I/O error, or passphrases did not match"},{"location":"commands/vault/#security-notes","title":"Security notes","text":"<ul> <li>The vault uses age's scrypt recipient, which is intentionally slow to   resist brute-force attacks.</li> <li>The passphrase is never written to disk or echoed to the terminal.</li> <li>The in-memory passphrase bytes are zeroed before the process exits.</li> </ul>"},{"location":"commands/vault/#see-also","title":"See also","text":"<ul> <li>password \u2014 store and retrieve SSH passwords</li> <li>host \u2014 manage SSH host entries</li> </ul>"}]}